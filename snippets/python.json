{
  "head file": {
    "prefix": "head",
    "body": [
      "#!/usr/bin/env python3",
      "# -*- coding: utf-8 -*-",
      "# author: askfiy",
      "# date: $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE"
    ],
    "description": "Python head file"
  },
  "no params function decorator": {
    "prefix": "decorator",
    "body": [
      "from functools import wraps\n\n",
      "def decorator(func):",
      "    @wraps(func)",
      "    def wrapper(*args, **kwargs):",
      "        print('before')",
      "        func(*args, **kwargs)",
      "        print('after')",
      "    return wrapper"
    ],
    "description": "Python decorator without parameters"
  },
  "dir object": {
    "prefix": "dir",
    "body": [
      "print(\"- \" + \"- \".join(list(map(lambda x: x+\"\\n\",",
      "\t\t\t\t\t\t\t\tfilter(lambda x: not x.startswith(\"_\"), dir($1))))))"
    ],
    "description": "Get object dir, Attributes that do not start with _"
  }
}
